@model IEnumerable<ShoppingList.Models.ShoppingListIndex>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Color)
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Color)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Shopping_ListID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Shopping_ListID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Shopping_ListID })
        </td>
    </tr>
}

</table>
